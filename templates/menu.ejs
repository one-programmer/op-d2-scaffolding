{
  title: '<%= program.title %>',
  icon: 'folder',
  children: [
    <%_ templateData.forEach(function(template, index){ _%>
    <%_ if (template.icon) { _%>
    { path: '<%= template.path %>', title: '<%= template.title %>', icon: '<%= template.icon %>' }<%= index === templateData.length ? '' : ',' %>
    <%_ } _%>
    <%_ }) _%>
  ]
}